services:
  - type: web
    name: hoikuen-line-reminder
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_PATH
        value: /var/data/app.db
      - key: DEFAULT_FAMILY_ID
        value: fam-demo
      - key: LINE_CHANNEL_ACCESS_TOKEN
        sync: false
      - key: LINE_CHANNEL_SECRET
        sync: false
      - key: CRON_SECRET
        generateValue: true
    disks:
      - name: sqlite-data
        mountPath: /var/data
        sizeGB: 1
